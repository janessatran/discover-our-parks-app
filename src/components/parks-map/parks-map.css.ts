import { css } from "lit";

export const styles = css`
  :host {
    display: flex;
    flex-direction: row;
  }

  .main-layout {
    display: flex;
    flex-direction: row;
    min-height: 100vh;
  }
`;
